{
  "name": "paper-radar-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Holds the LLM key server-side so the static site never carries one.",
  "engines": { "node": ">=20.6" },
  "scripts": {
    "start": "node --env-file=.env server.mjs",
    "test": "node --test"
  }
}
